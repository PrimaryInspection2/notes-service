apiVersion: v1
kind: Service
metadata:
  namespace: notes-app
  name: postgres-service
  labels:
    app: postgres
spec:
  # ClusterIP = internal only, postgres must not be exposed externally
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432