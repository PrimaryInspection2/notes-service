spring:
  datasource:
    username: note-service-local-user
    password: note-service-local-password
    url: jdbc:postgresql://localhost:5678/notes-service

server:
  port: 9000

logging:
  level:
    com.saveit: DEBUG
    org.springframework: DEBUG
